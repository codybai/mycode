<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tinydeer.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
      
      
      

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- 在SDCard中创建与删除文件权限   -->
    <!-- <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>   -->
    <!--  往SDCard写入数据权限  -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>   -->
    <!--   从SDCard读取数据权限  -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>   -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/timg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name="com.tinydeer.activity.OKHttpActivity" />
        <activity android:name="com.tinydeer.activity.OKHttpListActivity" />
        <activity android:name="com.tinydeer.activity.DetailActivity" />
        <activity android:name="com.tinydeer.activity.SendPostActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".RegisterActivity" />
        <activity android:name="com.tinydeer.activity.UserInfoPageActivity" />
        <activity android:name="com.tinydeer.activity.UserInfoSendStoreActivity" />
        <activity android:name="com.tinydeer.activity.WriteReplyActivity" />
        <activity android:name="com.tinydeer.activity.ShowUserListActivity" />
        <activity android:name="com.tinydeer.activity.ShowBoardActivity" ></activity>
        <activity android:name="com.tinydeer.activity.DetailNoticeActivity"></activity>
    </application>

</manifest>